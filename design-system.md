# 割烹 沙羅雙樹 — デザインシステム / スタイルガイド

方向性: **漆黒弁柄 — ダーク没入シアター**
対象: 京都・東山の割烹 沙羅雙樹（さらそうじゅ）LP
出典: 磨き込み済み最終プロトタイプ `proto-b.html`

---

## 1. デザインコンセプト

**「暗がりに、一皿だけが浮かぶ」** ——料理を舞台の主役に据えた、ダーク没入シアター。

照明はカウンターの手元と皿の上だけ、という割烹の空間体験をそのまま画面へ翻訳する。基調は濡羽墨（ぬればすみ）の漆黒。そこへ弁柄（べんがら）の朱がスポットライトのように一点だけ灯り、燻し金のヘアラインが舞台の幕（縦罫）を引く。生成りの文字が闇に静かに浮かぶ。

設計原則は割烹の哲学「引き算の、もてなし」と同義:

- **闇を主役にする** — 余白は白ではなく闇。背景の暗さが、文字・数値・一皿の存在を際立たせる。
- **朱は一点だけ** — 弁柄はアクセント10%。多用せず、CTA・主役（夜コース）・罫線の灯りにのみ置く。
- **静けさ＝贅沢** — 賑やかな装飾を排し、ヘアライン・グレイン・スローなブリージンググローで気配だけを残す。
- **声高に語らない** — 大書きせず、和文の組版美と余白で品格を立てる。

---

## 2. カラーパレット

ダーク基調。背景60% / 面30% / 朱アクセント10% を目安に配分する。

| 役割 | トークン | HEX | 用途 / 使用比率 | コントラスト注記 |
|---|---|---|---|---|
| Surface (base) | `sumi` 濡羽墨 | `#14110F` | ページ背景。**約60%** | 地色 |
| Surface (panel) | `tan` 炭墨 | `#211C18` | パネル・カード・献立表。**約30%** | 地色 |
| Text (body) | `kinari` 生成り | `#E8DECF` | 本文・見出し。最主要文字色 | sumi地で十分なAAA級 |
| Text (sub) | `tint` | `#A89A8B` | 補助本文・説明文 | tan地 5.6:1 / sumi地 6.3:1（AA） |
| Text (muted) | `tint-muted` | `#8C7F72` | 装飾ラベル・「休」記号・placeholder | 装飾専用（本文には使わない） |
| Text (note) | `note` | `#9A8C7D` | 注記・コピーライト | AA確保の不透明明色 |
| Accent (意匠) | `bengara` 弁柄 | `#9E3B2E` | 罫線・グロー・図版の朱。**約10%** | 意匠用（小文字テキスト不可） |
| Accent (text) | `bengara-text` 朱 | `#C75A4A` | 必須印`*`・夜の縦書き等 | 墨地 4.6:1（AA） |
| Line / 強調 | `kin` 燻し金 | `#B6925C` | ヘアライン・数値・ラベル・focus | hairlineは透過 28% で使用 |

**CTAボタン専用色:** 地 `#B04434` / hover `#9E3B2E` / 文字 `#F4EEE2`。

**運用ルール:**
- 朱（`bengara` / `bengara-text`）は「一点豪華」。1画面内に複数の朱面を置かない。
- 罫線は `kin` を `rgba(182,146,92,0.12〜0.45)` の透過で。実線の燻し金ベタは避ける。
- 選択範囲（`::selection`）は弁柄40%透過。

---

## 3. タイポグラフィ

4書体構成。和文ディスプレイ・和文本文・欧文ラベル・数値で役割を分ける。

| 役割 | フォント | トークン | weight | 用途 |
|---|---|---|---|---|
| 和文ディスプレイ | Zen Old Mincho | `--font-display` | 500（見出し）/400 | 見出し・ロゴ・店名 |
| 和文本文 | Zen Kaku Gothic New | `--font-body` | **300** | 本文・UIラベル・フォーム |
| 欧文ラベル | EB Garamond (italic) | `--font-latin` | italic | "Concept" 等の section eyebrow |
| 数値 | Cormorant | `--font-num` | 400 | 価格・電話・年数（tabular-nums） |

**スケール表（clamp で流体）:**

| 階層 | サイズ | line-height | 補足 |
|---|---|---|---|
| H1 (hero) | `clamp(3rem, 8vw, 5.5rem)` | 1.18 | weight 500, tracking 0.08em |
| H2 (`.h2-scale`) | `clamp(2rem, 4.4vw, 3rem)` | 1.25〜1.32 | weight 500 |
| H2 (reserve) | `clamp(2rem, 5vw, 3.4rem)` | 1.3 | |
| H3 | 1.2〜1.7rem | tracking 0.06〜0.08em | |
| Lede / 本文 | 1.0〜1.16rem | **1.78**（`.lede`） | tracking 0.02em |
| 本文標準 | 0.95〜1.02rem | **1.72**（body） | |
| ラベル/注記 | 0.74〜0.95rem | 1.7〜1.78 | tracking 0.04〜0.1em |
| 価格(大) | 2.6rem | 1.0 | `.num` |

**行間・字間の規約:**
- 本文 line-height は 1.72、長文リード（`.lede`）は 1.78 で統一。
- 大見出しほど tracking を広げる（0.06〜0.12em）。縦書きSCROLL等は 0.3em。
- 数値は `tabular-nums` + `tnum` を必ず適用（価格の桁揃え）。`¥` は字後 0.12em 空ける。

**和文組版の注意:**
- 全体に `font-feature-settings: "palt" 1`（プロポーショナル詰め）を適用。約物のアキを詰めて密度を整える。
- `text-rendering: optimizeLegibility` と `-webkit-font-smoothing: antialiased` で暗背景の細文字を最適化。
- 行長は最大 `38〜44ch` に抑える（`max-w-[40ch]` 等）。読みやすさと品格の両立。
- 縦書き（`writing-mode: vertical-rl; text-orientation: upright`）は「昼/夜」「SCROLL」等の意匠アクセントに限定使用。

---

## 4. スペーシングスケール & レイアウト規約

**スペーシング: 4 / 8 ベースのスケール**
`4, 8, 12, 16, 24, 32, 48, 64, 96, 128, 160`（px）。

**グリッド:**
- 標準コンテナ最大幅 **1280px**（献立 1180px / 予約 920px）。
- 左右パディング: モバイル `20px (px-5)` / デスクトップ `40px (px-10)`。
- セクションは 12カラムグリッド（`lg:grid-cols-12`）で割付。設えセクションは 6カラムの bento（非等幅・`auto-rows` 180〜200px）。

**セクション余白（縦）:**
- 標準: モバイル `96px` / デスクトップ `140px`。
- 予約: `100px` / `150px`。
- ヘッダー高 `68px`、ヒーローは `min-h-100svh`。

**行長:** 本文は `38〜44ch`、補助テキストは `30〜36ch` を上限に。中央寄せ導入文は `34ch` 前後。

**舞台演出のレイアウト要素:**
- `stage-rule`: 上下フェードする縦1pxの燻し金ヘアライン。`left: 16% / 84% / 50%` 等に配し「舞台の幕」を演出。
- `spotlight`: `4/5` のラジアルグラデ面。中央上から生成り光が差し、下端が闇に沈む。

---

## 5. 角丸・影・線幅トークン

**角丸:**
- カード/パネル: `--radius-card: 2px`（ごく僅か。直線基調を保つ）。
- ボタン/フォーム: `--radius-btn: 0px`（矩形。凛とした構え）。

**線幅（ヘアライン）:** 基本 1px。透過で階調を作る。
- 標準罫線: `rgba(182,146,92,0.28)`
- ghostボタン枠: `rgba(182,146,92,0.45)`
- パネル枠: `rgba(182,146,92,0.12)`
- リスト区切り: `rgba(168,154,139,0.12)`（tint/12）
- 図版の線（皿・地図）は 0.6〜1.4px、opacity 0.25〜0.7 で繊細に。

**影:** 浮遊感は最小限。影より「光（ラジアルグロー）」で奥行きを作るのが原則。
- sticky予約バー: `0 -1px 0 rgba(182,146,92,0.35), 0 -18px 40px rgba(0,0,0,0.55)`
- フォーカスリング: `0 0 0 2px rgba(182,146,92,0.22)`

---

## 6. アイコン・イラストのトーン規約

- **線画ベース。** stroke-width 1.2〜1.8、塗りは原則なし（fill="none"）。角・端は `round`。
- 配色は燻し金 `#B6925C`（主）・弁柄 `#9E3B2E`（差し色）・生成り `#E8DECF`（ハイライト）の3色に限定。
- 料理・器・室・地図は**抽象的なインラインSVGの線画**で表現（写真差し替え時も spotlight 枠ごとに機能する設計）。
- 装飾アイコンは `aria-hidden="true"`。意味を持つSVGには `aria-label`。
- グレイン（`grain`）はSVGノイズを opacity 0.04 / `mix-blend-mode: overlay` で薄く重ね、フィルムのような質感を付与。
- 雰囲気は「余白に灯る最小の意匠」。情報過多にせず、線は数本で要点だけ示す。

---

## 7. インタラクション規約

**Transition:**
- a / button の基本: `color, background-color, border-color, opacity 200ms ease`。
- リンク下線（`link-underline`）: `transform 280ms ease`、左から `scaleX` で伸びる燻し金の1px。
- フォーム: `border-color, box-shadow 200ms ease`。
- reveal（出現）: `opacity, transform 900ms cubic-bezier(.2,.6,.2,1)`、22px下から立ち上がる。
- sticky予約バー: `transform 420ms cubic-bezier(.2,.7,.2,1)`、スクロール240px超で下からせり上がる。

**Hover:**
- リンクは下線が伸び、文字色が `tint → kinari` に明るくなる。
- 弁柄ボタン: `#B04434 → #9E3B2E`。ghostボタン: 枠が `kin` 化＋ `rgba(182,146,92,0.08)` の薄い灯り。

**Focus:**
- `:focus-visible` に `outline: 2px solid var(--color-kin); outline-offset: 3px`。
- フォーム/ラジオ/ステッパーは燻し金の枠＋グローで状態を明示。最小タップ領域 44px（`tap44` / `min-height: 44〜48px`）。

**モーション原則:**
- スローで没入的（reveal 900ms、hero グロー `breathe` 14s）。急峻な動きを避ける。
- `prefers-reduced-motion: reduce` を全面尊重——reveal即時表示・ambient停止・smooth scroll無効化。
- 動きは「灯る/浮かぶ/呼吸する」のメタファーに統一。弾む・回転は使わない。

---

## 8. ライティングトーン

- **語調:** 静謐・端正・控えめ。敬体（です・ます）で品位を保ち、過度な誇張や感嘆符を使わない。
- **一人称の所作:** 「〜を承ります」「〜をお供します」「〜をしつらえます」など、もてなしの所作で締める。
- **比喩は和の情景で:** 「夜の帳が下りるころ、一椀が灯る」「白い牡丹の一皿」「初夏の川面が薫る」。視覚・嗅覚に訴える京の季語を織り込む。
- **数値は明快に、空気は余白で:** 価格・時間・席数は曖昧にせず明記（例「全十品 ・ 約150分」「八席」）。一方で情緒は短い体言止めで余韻を残す。
- **専門語にはやさしい補助を:** 「先付」「八寸」等の献立語は、副えの料理名で具体を補う。
- **禁則:** 煽り表現・絵文字・ビックリマーク・カタカナ過多を避ける。沈黙と省略を恐れない。
